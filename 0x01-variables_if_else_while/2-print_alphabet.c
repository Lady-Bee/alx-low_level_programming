#include <stdio.h>
/**
 * main - This is the main function
 *
 * Return: always returns 0
 */

int main(void)
{
	char i = 'a';

	for (i = 'a'; i <= 'z'; i++)

	putchar(i);
	putchar('\n');

	return (0);
}
