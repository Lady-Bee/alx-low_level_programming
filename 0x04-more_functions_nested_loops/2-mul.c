#include "main.h"

/**
 * mul - This program prints putchar followed by a new line
 *@a: first parameter to be checked
 *@b: second parameter to be checked
 * Return: returns 0 when successful
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
		return (result);
}
